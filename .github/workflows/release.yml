name: Release

# Push a tag that matches package.json's version (e.g. v0.1.0) and every
# platform's installers are built and attached to a GitHub pre-release.
# Review it and, when it is ready, edit it into a full release by hand.

on:
  push:
    tags: ['v*.*.*']
  workflow_dispatch:
    inputs:
      tag:
        description: Existing tag to (re)build, e.g. v0.1.0
        required: true

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

env:
  NODE_VERSION: '24'
  TAG: ${{ inputs.tag || github.ref_name }}

jobs:
  prepare:
    name: check version & create pre-release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ env.TAG }}
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Tag matches package.json
        run: |
          version="v$(node -p "require('./package.json').version")"
          if [ "$version" != "$TAG" ]; then
            echo "::error::tag $TAG does not match package.json version $version"
            exit 1
          fi
      - run: npm ci
      - run: npm run verify
      # One release up front: parallel package jobs would otherwise race to create it.
      # The installers are unsigned, so the notes open with what to do when Windows or
      # macOS warns - the same steps as the README's "If a warning appears", kept short
      # here, in English and Japanese - and GitHub appends the generated changes below them.
      - name: Create pre-release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if gh release view "$TAG" >/dev/null 2>&1; then exit 0; fi
          cat > "$RUNNER_TEMP/notes.md" <<'EOF'
          ## Installing: the builds are unsigned

          Windows and macOS warn before the first run because the installers are not code-signed. The warning means the publisher is unknown to them, not that anything was found in the file. Each file's SHA-256 is listed beside it below. Full steps: [README → If a warning appears](https://github.com/kurouna/elecmockolla#if-a-warning-appears).

          - **Windows**: if the browser holds the download back, choose *Keep* (Edge: *⋯ → Keep → Show more → Keep anyway*). SmartScreen then shows "Windows protected your PC": click **More info → Run anyway**. The installer is per-user and asks for no administrator rights. The `.zip` runs without installing.
          - **macOS 15+**: open the app once and click **Done**. Then go to **System Settings → Privacy & Security → Open Anyway**. On macOS 14 or earlier, right-click the app and choose **Open**. If macOS says the app "is damaged", run `xattr -dr com.apple.quarantine /Applications/elecmockolla.app`.
          - **Linux**: no warning. On Ubuntu 24.04+ prefer the **.deb**, which installs the AppArmor profile Electron's sandbox needs; the AppImage needs `chmod +x` and `libfuse2` (`libfuse2t64`). Never run it with `--no-sandbox`.

          The mock listens on `127.0.0.1:11434`, Ollama's port. If Ollama runs on the same machine, change the port in **Settings**.

          macOS and Linux builds pass the unit tests on GitHub's runners but have had no hands-on testing yet.

          ## インストール：ビルドは未署名です

          インストーラーにコード署名がないため、Windows と macOS は初回の起動前に警告を出します。警告は発行元が分からないという意味で、ファイルに何かが見つかったわけではありません。各ファイルの SHA-256 は下の一覧に表示されています。詳しい手順は [README → If a warning appears](https://github.com/kurouna/elecmockolla#if-a-warning-appears)（英語）にあります。

          - **Windows**：ブラウザーがダウンロードを止めたら「保存」を選びます（Edge は「⋯ → 保存 → 詳細表示 → 保持する」）。SmartScreen の「Windows によって PC が保護されました」が出たら「**詳細情報 → 実行**」を押します。インストールはユーザー単位で、管理者権限は求めません。`.zip` はインストールせずに使えます。
          - **macOS 15 以降**：アプリを一度開いて「**完了**」を押し、「**システム設定 → プライバシーとセキュリティ → このまま開く**」を押します。macOS 14 以前は、アプリを右クリックして「**開く**」を選びます。「壊れているため開けません」と出たら `xattr -dr com.apple.quarantine /Applications/elecmockolla.app` を実行します。
          - **Linux**：警告は出ません。Ubuntu 24.04 以降では **.deb** を使ってください（Electron のサンドボックスに必要な AppArmor のプロファイルも入ります）。AppImage は `chmod +x` と `libfuse2`（`libfuse2t64`）が必要です。`--no-sandbox` では起動しないでください。

          モックは Ollama と同じ `127.0.0.1:11434` で待ち受けます。同じ PC で Ollama を動かしている場合は「**設定**」でポートを変えてください。

          macOS 版と Linux 版は GitHub のランナーで単体テストを通していますが、実機での確認はまだです。

          EOF
          gh release create "$TAG" --prerelease --verify-tag --title "elecmockolla ${TAG#v}" \
            --notes-file "$RUNNER_TEMP/notes.md" --generate-notes

  package:
    name: publish (${{ matrix.os }} ${{ matrix.arch }})
    needs: prepare
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            arch: x64
          - os: ubuntu-24.04-arm
            arch: arm64
          - os: windows-latest
            arch: x64
          # No native modules, so these cross-package on the same runners.
          - os: windows-latest
            arch: arm64
          - os: macos-latest
            arch: arm64
          - os: macos-latest
            arch: x64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ env.TAG }}
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - name: Build and publish to the pre-release
        run: npm run package -- --${{ matrix.arch }} --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # electron-builder skips a published release older than two hours; a slow
          # job or a manual rebuild of an older tag must still be able to upload.
          EP_GH_IGNORE_TIME: 'true'
